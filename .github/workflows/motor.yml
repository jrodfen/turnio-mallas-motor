name: Actualizador Nocturno Mallas Renfe

on:
  schedule:
    - cron: '0 3 * * *' 
  workflow_dispatch: 

permissions:
  contents: write

jobs:
  procesar-mallas:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar el código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Descarga todo el historial para evitar errores de pull

      - name: Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Instalar herramientas
        run: npm install

      - name: Ejecutar Extractor y Optimizador
        run: npm start

      - name: Guardar los JSONs (Limpieza Total)
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m "Actualización automática Mallas Renfe" || echo "No hay cambios"
          git push origin main --force
